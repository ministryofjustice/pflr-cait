<div class="c c-proscons">
  <% if (page.pros) { %>
  <div class="c-proscons__pros">
    <h2>Pros:</h2>
    <ul>
      <% page.pros.forEach(function(pro, i) { %>
        <li class="icon-tick"><%- pro.text %></li>
      <% }) %>
    </ul>
  </div>
  <% } if (page.cons) { %>
  <div class="c-proscons__cons">
    <h2>Cons:</h2>
    <ul>
      <% page.cons.forEach(function(con, i) { %>
        <li class="icon-cross"><%- con.text %></li>
      <% }) %>
    </ul>
  </div>
<% } %>
</div>
