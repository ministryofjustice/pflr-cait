/* global $ */

// No longer required

// if ($('.u-card').length) {
//   let card = $('.u-card')
//   for (var i = 0;i < card.length;i++) {
//     if(i % 2 === 0) {
//       // first item in each row.
//       let thisCard = $(card[i]);
//       let nextCard = $(card[i+1]);
//       let tCHeight = thisCard.outerHeight();
//       let nCHeight = nextCard.outerHeight();
//       if( tCHeight >= nCHeight) {
//         thisCard.css('height', tCHeight+'px');
//         nextCard.css('height', tCHeight+'px');
//       } else {
//         nextCard.css('height', nCHeight+'px');
//         thisCard.css('height', nCHeight+'px');
//       }
//     }
//   }
//};
