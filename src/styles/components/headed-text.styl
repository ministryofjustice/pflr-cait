&-headed-text {
  margin-bottom: $vspacing;

  h1 {
    font-size: $mantle;
    margin-bottom: $vspacing;
  }

  p {
    ff-body();
  }
  .u-primary & {
    h1 {
      font-size: $crust;
      font-size: 4.8rem;
      margin-top: 4rem;
    }
    p:first-of-type {
      font-size: $baseFont*2.4;
    }
  }

}
