&-article-list {
  clear:both;
  padding-top: $vspacing*2;
  h2 {
    padding: $spacing 0;
    font-size: $crust;
  }
  &__summary {
    cf();
    padding: $spacing;
    margin-bottom: $vspacing*2;
    h1 {
      a {
        color: $links;
      }
    }

  }
  &.updating > * {
    opacity: 0.25;
  }
}
